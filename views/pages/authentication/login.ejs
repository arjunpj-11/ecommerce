<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Sign in - ARNI</title>
  <link rel="stylesheet" href="/stylesheets/authentication/login.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <link rel="stylesheet" href="/stylesheets/responsive.css">
</head>
<body>
  <canvas id="backgroundCanvas"></canvas>
  
  <div class="login-container">
    <div class="login-box">
      <div class="logo-section">
        <img src="/images/common/logo.png" alt="ARNI">
      </div>
      <div class="image-section">
        <img src="/images/authentication/signin image.jpg" alt="Login Image">
      </div>
      <div class="form-section">
        <h2>Welcome back</h2>
        <p class="auth-intro">Sign in to continue to your ARNI account.</p>
        <form id="form">
          <% if (emailError) { %>
            <input type="text" name="emailOrPhone" placeholder="Email or Phone Number" id="emailOrPhone" value="<%= detail.emailOrPhone %>" autocomplete="username" required>
            <p class="validation iValid" id="emailOrPhoneMessage"><%=emailError%></p>
          <% } else { %>
            <input type="text" name="emailOrPhone" placeholder="Email or Phone Number" id="emailOrPhone" value="<%= detail.emailOrPhone %>" autocomplete="username" required>
            <p class="validation iValid" id="emailOrPhoneMessage"></p>
          <% } %>
        
          <!-- Password Input -->
          <div class="password-wrapper">
            <input type="password" name="password" placeholder="Password" id="password" autocomplete="current-password" required>
            <i class="fas fa-eye" id="togglePassword"></i>
          </div>
          <p class="validation iValid" id="passwordMessage"><%=error%></p>

          <button type="submit">Login</button>
        </form>
        
        <!-- Forgot Password and Sign Up Links -->
        <div class="form-links">
          <a href="/auth/forgetPassword" class="forgot-password">Forgot Password?</a>
          <span> | </span>
          <a href="/auth/signin" class="signup">Sign Up</a>
        </div>
        
        <div class="social-login">
          <p>Or continue with</p>
          <a href='/api/google/auth/google' class="btn google">
            <i class="fab fa-google"></i> Google
          </a>
          <a href="/api/facebook/auth/facebook" class="btn facebook">
            <i class="fab fa-facebook"></i> Facebook
          </a>
        </div>
      </div>
    </div>
  </div>
  <script src="/javascripts/authentication/login.js"></script>
    <script src="/javascripts/responsive.js"></script>
</body>
</html>
